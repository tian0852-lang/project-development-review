interface:
  display_name: "project-development-review"
  short_description: "Review designs and prepare version-approved handoffs"
  default_prompt: "Use $project-development-review to review this project through approved technical handoff only; do not implement business code."
policy:
  allow_implicit_invocation: true
