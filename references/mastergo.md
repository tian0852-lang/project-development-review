# MasterGo

Use only when MasterGo is selected. Capability is established by actual current tool returns, not by a general claim that a connector always supports a feature. Read selected artboard IDs, names, dimensions and static structure when available.

If a parent cannot reliably enumerate artboards, request direct multi-selection, an explicit artboard list or individual layer_id links. Do not count screenshot silhouettes as confirmed pages. Static-only evidence never confirms resend actions, parameters, return retention, dialog behavior or annotation authors. User text/recordings may supply these facts as user_provided evidence.
