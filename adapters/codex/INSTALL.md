# Codex adapter — 2.2.1

Copy the complete package, only with installation authorization, to `.agents/skills/project-development-review/` in the intended repository. Personal scope is `~/.agents/skills/project-development-review/` in current documentation. Invoke `$project-development-review` or select it through `/skills` where exposed. [Official local Skills guidance](https://learn.chatgpt.com/docs/build-skills)

This upgrade does not replace the existing personal v1 copy, including any legacy `~/.codex/skills/` copy. Check the selected file path/version to avoid ambiguous same-name Skills.

Merge [AGENTS.md.template](AGENTS.md.template) into the applicable project AGENTS.md; never overwrite unrelated rules. Record the full Skill path. Use [invocation.md](invocation.md) to supply roots/evidence and start the current phase. Persistent guidance does not grant business-code permission.

If discovery is unavailable, read the package from an explicit local path or attach the required materials and use the same invocation prompt with its fallback. Mark text-only environments accurately. Documentation checked 2026-08-31; fresh v2 installation/invocation: not run.
