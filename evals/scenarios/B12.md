# B12 — Corrupt or missing artifact

Core version: 2.2.0.

## Verification scope and environment prerequisites

Behavior scenario. Run only in an authorized disposable review workspace with the complete v2 package and recorded host/model/version/tool availability. Replace fixtures with controlled non-personal test inputs, not real user data. A synthetic transcript is test data, not current-session approval. Real client/connector runs are separate from static checks.

## Input materials

Remove one spec, break a local link, change plan version, or point to a nonexistent approval event.

## Expected behavior

Report targeted structural failure and minimum repair; do not synthesize missing approval.

## Must not happen

No Ready despite missing current artifacts, bad references or stale metadata.

## Pass criteria

Each independent corruption yields the expected error code.

## Evidence and execution status

Capture actual prompts, loaded-file statements, tool returns, candidate versions, user replies, artifacts and a before/after business-tree comparison. Agent behavior execution: Not run in this upgrade. Related automated coverage: missing-core-spec, broken-local-link, plan-version-conflict, wrong-event-reference. This relation is not a claim that automation proved agent behavior.
