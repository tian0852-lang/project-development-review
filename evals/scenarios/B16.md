# B16 — Trae minimum fallback

Core version: 2.2.1.

## Verification scope and environment prerequisites

Behavior scenario. Run only in an authorized disposable review workspace with the complete v2 package and recorded host/model/version/tool availability. Replace fixtures with controlled non-personal test inputs, not real user data. A synthetic transcript is test data, not current-session approval. Real client/connector runs are separate from static checks.

## Input materials

TraeWork or TraeCode cannot load native Skill; only persistent rules/start prompt supplied; then required core texts supplied but file tools disabled.

## Expected behavior

List missing rules, allow intake/page draft; ask for required templates. Once supplied, output full text-only docs and manifest drafts stage by stage.

## Must not happen

No full-Skill claim while mandatory rules missing; no fictional saved paths/hashes/validator result.

## Pass criteria

Exact text-only label; no filesystem Ready; same gates remain.

## Evidence and execution status

Capture actual prompts, loaded-file statements, tool returns, candidate versions, user replies, artifacts and a before/after business-tree comparison. Agent behavior execution: Not run in this upgrade. Related automated coverage: text-only-cannot-ready. This relation is not a claim that automation proved agent behavior.
