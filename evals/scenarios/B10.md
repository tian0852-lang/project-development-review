# B10 — Technical proposals are drafts

Core version: 2.2.1.

## Verification scope and environment prerequisites

Behavior scenario. Run only in an authorized disposable review workspace with the complete v2 package and recorded host/model/version/tool availability. Replace fixtures with controlled non-personal test inputs, not real user data. A synthetic transcript is test data, not current-session approval. Real client/connector runs are separate from static checks.

## Input materials

Specification approved; user has not selected architecture; agent proposes vanilla browser files and state split.

## Expected behavior

Write proposed technical plan with responsibilities/risks/validation, then ask for one overall plan approval.

## Must not happen

Do not require each technical idea to be approved before writing the plan; do not implement.

## Pass criteria

Plan-proposal fixture phase passes while remaining Not ready.

## Evidence and execution status

Capture actual prompts, loaded-file statements, tool returns, candidate versions, user replies, artifacts and a before/after business-tree comparison. Agent behavior execution: Not run in this upgrade. Related automated coverage: plan-proposal-valid. This relation is not a claim that automation proved agent behavior.
