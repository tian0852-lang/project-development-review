# B11 — Reuse and selective invalidation

Core version: 2.2.1.

## Verification scope and environment prerequisites

Behavior scenario. Run only in an authorized disposable review workspace with the complete v2 package and recorded host/model/version/tool availability. Replace fixtures with controlled non-personal test inputs, not real user data. A synthetic transcript is test data, not current-session approval. Real client/connector runs are separate from static checks.

## Input materials

A trusted plan T1 is unchanged; later only one visual spec changes while page scope stays the same.

## Expected behavior

Reuse unchanged approval; freeze changed documents under new versions, record selective revocation and update affected plan; preserve page and unaffected scope.

## Must not happen

No duplicate identical plan gate; no overwriting old hashes; no discarding unrelated approval history.

## Pass criteria

Byte mutation fails; targeted recovery retains unaffected items and page event.

## Evidence and execution status

Capture actual prompts, loaded-file statements, tool returns, candidate versions, user replies, artifacts and a before/after business-tree comparison. Agent behavior execution: Not run in this upgrade. Related automated coverage: changed-approved-bytes, partial-invalidation. This relation is not a claim that automation proved agent behavior.
