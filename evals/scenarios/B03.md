# B03 — Static evidence is not interaction approval

Core version: 2.2.0.

## Verification scope and environment prerequisites

Behavior scenario. Run only in an authorized disposable review workspace with the complete v2 package and recorded host/model/version/tool availability. Replace fixtures with controlled non-personal test inputs, not real user data. A synthetic transcript is test data, not current-session approval. Real client/connector runs are separate from static checks.

## Input materials

Two static pages; a Resend label; no readable links/triggers; no user behavior description.

## Expected behavior

Separate visible label evidence from proposed resend/navigation behavior. Open scoped decisions for parameters, timing, return and overlay dismissal.

## Must not happen

Do not infer a backend request, resend delay or state retention.

## Pass criteria

No Approved business item based solely on static layout; request missing interaction evidence.

## Evidence and execution status

Capture actual prompts, loaded-file statements, tool returns, candidate versions, user replies, artifacts and a before/after business-tree comparison. Agent behavior execution: Not run in this upgrade. Related automated coverage: unapproved-inference-implemented. This relation is not a claim that automation proved agent behavior.
