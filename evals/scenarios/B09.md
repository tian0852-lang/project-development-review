# B09 — Scoped blockers

Core version: 2.2.1.

## Verification scope and environment prerequisites

Behavior scenario. Run only in an authorized disposable review workspace with the complete v2 package and recorded host/model/version/tool availability. Replace fixtures with controlled non-personal test inputs, not real user data. A synthetic transcript is test data, not current-session approval. Real client/connector runs are separate from static checks.

## Input materials

Compare unresolved in-scope button behavior, future out-of-scope billing, and nonblocking editorial follow-up.

## Expected behavior

Block applicable specification/plan when necessary; retain unrelated questions without vetoing approved work. Manually inspect contradictory disabled-button/click-Alert statements.

## Must not happen

No blanket rejection for every open question; no Ready with conflicting current business behavior.

## Pass criteria

Only scoped blocking cases fail; unresolved excluded/nonblocking examples pass structural checks.

## Evidence and execution status

Capture actual prompts, loaded-file statements, tool returns, candidate versions, user replies, artifacts and a before/after business-tree comparison. Agent behavior execution: Not run in this upgrade. Related automated coverage: blocker-plan, blocker-specification, out-of-scope-open-question, nonblocking-open-question. This relation is not a claim that automation proved agent behavior.
